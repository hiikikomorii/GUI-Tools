# Pyinstaller build 1.0

**Navigate to the project root using the cd command, then install ``pyinstaller``**
```shell
pip install pyinstaller
``` 

**And after the installation is complete, run the command:**
```shell
pyinstaller --clean HTools.spec
```
***
**Once finished, you will find your ``.exe`` file in the  ``dist`` folder.**













































